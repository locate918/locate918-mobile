node_modules/
android/app/build/
android/.gradle/
.gradle/
build/
.metro-health-check*
*.hprof
.env

# Release signing — never commit upload keystores or secrets.
# (debug.keystore is intentionally tracked and remains so.)
*.jks
*.keystore
!android/app/debug.keystore
android/keystores/
